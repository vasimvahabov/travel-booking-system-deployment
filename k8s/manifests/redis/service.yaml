apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: travel-booking-system
spec:
  selector:
    db: redis
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
