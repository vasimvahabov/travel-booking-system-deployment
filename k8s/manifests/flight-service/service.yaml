apiVersion: v1
kind: Service
metadata:
  name: flight-service
  namespace: travel-booking-system
spec:
  type: ClusterIP
  selector:
    app: flight-service
  ports:
    - port: 80
      targetPort: 8899
      protocol: TCP
