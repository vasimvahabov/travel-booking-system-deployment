apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postgres.fullname" . }}-init
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
data:
{{- $file := "configurations/init.sql" }}
  {{ base $file }}: |-
{{- .Files.Get $file | nindent 4 }}
