apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-server.fullname" . }}
  labels:
    {{- include "edge-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edge-server.selectorLabels" . | nindent 4 }}
