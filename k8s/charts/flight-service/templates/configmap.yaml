apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flight-service.fullname" . }}-config
  labels:
    {{- include "flight-service.labels" . | nindent 4 }}
data:
{{- $file := "configurations/application.yaml" }}
  {{ base $file }}: |-
{{- .Files.Get $file | nindent 4 }}
